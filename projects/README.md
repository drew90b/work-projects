# Projects

One markdown file per active project. A project is any desired outcome requiring more
than one action step. Each file uses the template at `templates/project.md` and must
always have a current **Next Action** field — if it doesn't, add one before closing the
file. Review this folder during your morning GTD review and update `next-actions/`.
