# Archive

Completed items from `boss-requests/`, `projects/`, and `maintenance-requests/` are
moved here when done. Never delete — always archive. Subfolders mirror the active
structure: `projects/`, `boss-requests/`, `maintenance-requests/`. Files are moved via
`git mv` to preserve history.
