# Maintenance Requests

Incoming maintenance work split into two subfolders: `active/` for work currently in
progress or newly received, and `follow-up/` for items waiting on parts, approvals, or
a return visit. Use `templates/maintenance-request.md` for each new request. When
complete, move to `archive/maintenance-requests/`.
